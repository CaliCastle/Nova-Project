using Nova;
using UnityEditor;
using UnityEngine;

namespace Example
{
    public sealed class Example : MonoBehaviour, INovaLaunchable
    {
        public void LiftOff( UIWindow window )
        {
            
        }
    }
}
